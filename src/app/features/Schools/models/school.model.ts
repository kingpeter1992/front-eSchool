export interface School {
  id: string;
  name: string;
  code: string;
  email: string;
  phone: string;
  logoUrl?: string;
  currency: string;
  timezone: string;
  domain: string;
  active: boolean;
}
